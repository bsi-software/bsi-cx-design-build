{% set linkPartId = 'link-1wfD2H' %}
<div class="bsi-template-button" {{ templateElement("template-button-3seC3G") }}>
    <a href="{{ linkUrl(linkPartId) }}" target="{{ ifLinkTarget(linkPartId, '_blank', '_self')}}" aria-label="{{ linkDescription(linkPartId) }}">{{ linkText(linkPartId) }}</a>
    <div>{{ textValue('multiline-plain-text-wmiRti') }}</div>
</div>
