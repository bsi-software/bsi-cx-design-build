{% set linkPartId = 'link-1wfD2H' %}
{# {% set scope = scope ? scope : 'root' %} #}
<div class="bsi-template-button" {{ templateElement("template-button-3seC3G", scope) }}>
    <a href="{{ linkUrl(linkPartId, scope) }}" target="{{ ifLinkTarget(linkPartId, scope, '_blank', '_self')}}" aria-label="{{ linkDescription(linkPartId, scope) }}">{{ linkText(linkPartId, scope) }}</a>
    <div>{{ textValue('multiline-plain-text-wmiRti', scope) }}</div>
</div>