{% set linkPartId = 'link-1wfD2H' %}
<div class="bsi-template-button" {{ templateElement("template-button-3seC3G", scope) }} scope={{scope}}>
    <a href="{{ linkUrl(scope, 'link-1wfD2H') }}" target="{{ ifLinkTarget(linkPartId, scope, '_blank', '_target') }}" {{ linkDescriptionAttr(linkPartId, scope) }}>{{ linkText(linkPartId, scope) }}</a> #}
    <div>{{ textValue('multiline-plain-text-wmiRti', scope) }}</div>
</div>